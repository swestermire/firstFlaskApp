(activities are not ordered in any special way)

Pre-Oct 25, 2016:
1) setup html files, main flaskApp file (FlaskApp#1), setup HTMLRequests (CRUD operations)
2) redirect, render_template
3) setup database model (database_setup.py)
4) setup SQLAlchemy... implemented sqlite for server practice
5) setup @app.route
6) implemented session.query
7) setup all page links using <a

Oct 25, 2016: 
1) linked bootstrap
2) linked css
3) created main.html that is extended and renders content via {% block content %} and  .html pages and renders header and footers in this way everything is uniform and inherits the same stylesheets
4) implemented jsonify (URL.../JSON)
5) setup flash messages